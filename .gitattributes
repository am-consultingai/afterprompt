# Always check out text files with LF line endings, including on Windows (Git for Windows defaults to CRLF).
# afterprompt.sh runs under bash, which fails on CRLF, and Windows users run it from WSL.
* text=auto eol=lf
*.png binary
