{
  "schema": 1,
  "about": "Every kind of environment Afterprompt looks for before it scans anything. An environment is one filesystem with its own home folder and its own AI tool history: a key pasted inside a WSL distribution or a devcontainer is invisible from the machine hosting it. The scan reports on every kind listed here — found and scanned, found and not scanned with the reason, or not present — so the coverage section answers \"what about X?\" without anyone having to ask.",
  "kinds": [
    {
      "id": "host",
      "label": "This machine",
      "platforms": ["windows", "macos", "linux", "wsl"],
      "detect": "always",
      "scan": "in-process",
      "note": "The filesystem the scan runs on."
    },
    {
      "id": "wsl",
      "label": "WSL distributions",
      "platforms": ["windows", "wsl"],
      "detect": "wsl.exe -l -q",
      "scan": "worker",
      "note": "Each distribution has its own home. Scanned from inside itself, because reading it from Windows over \\\\wsl.localhost is about fourteen times slower."
    },
    {
      "id": "docker",
      "label": "Docker containers",
      "platforms": ["windows", "macos", "linux", "wsl"],
      "detect": "docker ps -a",
      "scan": "copy",
      "note": "A container has its own home folder, and a devcontainer is where a lot of AI coding actually happens. The known AI tool paths are copied out read-only; nothing is executed inside the container."
    },
    {
      "id": "podman",
      "label": "Podman containers",
      "platforms": ["macos", "linux", "wsl"],
      "detect": "podman ps -a",
      "scan": "copy",
      "note": "Same as Docker, through the podman CLI."
    },
    {
      "id": "docker_image",
      "label": "Docker images",
      "platforms": ["windows", "macos", "linux", "wsl"],
      "detect": "docker images",
      "scan": "never",
      "why": "Reading an image means creating a container from it, which changes the machine's Docker state. Afterprompt lists them and leaves them alone.",
      "note": "An image can hold a key baked into a layer; the layer is visible to anyone who pulls it."
    },
    {
      "id": "devcontainer",
      "label": "Dev container configuration",
      "platforms": ["windows", "macos", "linux", "wsl"],
      "detect": ".devcontainer/devcontainer.json under the home folder",
      "scan": "via docker",
      "note": "The configuration says a container exists; the container itself is scanned as a Docker container when it is running."
    },
    {
      "id": "codespace",
      "label": "GitHub Codespace",
      "platforms": ["linux"],
      "detect": "$CODESPACES environment variable",
      "scan": "in-process",
      "note": "A Codespace is the machine the scan is running on, not another environment to reach."
    },
    {
      "id": "lima",
      "label": "Lima or Colima virtual machines",
      "platforms": ["macos"],
      "detect": "limactl list",
      "scan": "never",
      "why": "Reachable only over its own ssh, which Afterprompt does not do yet.",
      "note": "Colima is where Docker usually runs on a Mac, so its containers appear under Docker instead."
    },
    {
      "id": "multipass",
      "label": "Multipass virtual machines",
      "platforms": ["windows", "macos", "linux"],
      "detect": "multipass list",
      "scan": "never",
      "why": "Reachable only over its own shell, which Afterprompt does not do yet."
    },
    {
      "id": "other_home",
      "label": "Other accounts on this machine",
      "platforms": ["windows", "macos", "linux", "wsl"],
      "detect": "sibling folders beside this home",
      "scan": "never",
      "why": "Reading another account's files needs elevation, and Afterprompt never elevates.",
      "note": "Listed so the coverage is honest about whose history was not read."
    },
    {
      "id": "folder",
      "label": "Extra home folders",
      "platforms": ["windows", "macos", "linux", "wsl"],
      "detect": "AFTERPROMPT_TEST_ENVS",
      "scan": "worker",
      "note": "A mounted disk or a fixture, scanned through the same worker protocol a distribution uses."
    }
  ]
}
